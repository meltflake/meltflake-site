---
const base = import.meta.env.BASE_URL;
---

<header style="border-bottom: 1px solid var(--surface1); padding: 0.75rem 0; margin-bottom: 1.5rem;">
  <nav class="flex items-center justify-between flex-wrap gap-1">
    <a href={base} style="color: var(--green); font-weight: 700; font-size: 1.1rem;">
      &gt; CreatorCut_
    </a>
    <div class="flex gap-1 flex-wrap">
      <a href={base} style="color: var(--foreground1);">首页</a>
      <span style="color: var(--surface2);">│</span>
      <a href={`${base}/compare`} style="color: var(--foreground1);">对比</a>
      <span style="color: var(--surface2);">│</span>
      <a href={`${base}/calculator`} style="color: var(--foreground1);">计算器</a>
      <span style="color: var(--surface2);">│</span>
      <a href={`${base}/about`} style="color: var(--foreground1);">关于</a>
    </div>
  </nav>
</header>
